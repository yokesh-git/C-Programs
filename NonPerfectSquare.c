#include<stdio.h>
#include<math.h>
int main()
{
    float sq=0.0;
    int i=1;
    int N;
    scanf("%d",&N);
    while(1)
    {
        sq = sqrt(i);
        //printf("\nSq : %d ",sq
        int t = 10;
        printf(isdigit(t));
        if()
        {
            i++;
            continue;
        }
        else if(N == 0)
        {
            break;
        }
        else
        {
            printf("\n%d ",i);
            N--;
            i++;
        }

    }
    return 0;
}
